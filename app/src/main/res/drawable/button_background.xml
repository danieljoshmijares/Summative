<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#6200EE" /> <!-- Darker blue for pressed state -->
            <stroke android:width="2dp" android:color="#3700B3" /> <!-- Darker stroke color -->
            <corners android:radius="8dp" />
        </shape>
    </item>
    <item>
        <shape>
            <solid android:color="#6200EE" /> <!-- Darker blue for normal state -->
            <stroke android:width="2dp" android:color="#BB86FC" /> <!-- Slightly lighter stroke color -->
            <corners android:radius="8dp" />
        </shape>
    </item>
</selector>
